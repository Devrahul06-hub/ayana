---
interface Props {
  title?: string;
  description?: string;
  canonicalUrl?: string;
}

const {
  title = 'Ayana Technologies — AI-Native Software Development Studio',
  description = 'We build AI-native software where artificial intelligence is the foundation — not a feature bolted on later. Custom software, SaaS, mobile apps, and cloud infrastructure. Based in Pune, delivering globally.',
  canonicalUrl = 'https://ayanatechnologies.in/',
} = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="keywords" content="AI development, custom software development, SaaS development, mobile app development, cloud infrastructure, AI-native software, LLM integration, Pune software company, ayana technologies llp" />
    <meta name="robots" content="index, follow" />
    <meta name="author" content="Ayana Technologies LLP" />
    <meta name="theme-color" content="#08091A" />
    <link rel="canonical" href={canonicalUrl} />

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="Ayana Technologies LLP" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=Plus+Jakarta+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;1,400&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet" />

    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "ProfessionalService",
      "name": "Ayana Technologies LLP",
      "url": "https://ayanatechnologies.in",
      "email": "support@ayanatechnologies.in",
      "address": {
        "@type": "PostalAddress",
        "addressLocality": "Pune",
        "addressRegion": "Maharashtra",
        "addressCountry": "IN"
      },
      "description": "AI-native software development studio. We build software where artificial intelligence is the architecture — not an afterthought. Custom software, SaaS, mobile apps, and cloud infrastructure.",
      "serviceType": ["AI Development","Custom Software Development","SaaS Development","Mobile App Development","Cloud Application Development"],
      "areaServed": "Worldwide",
      "foundingLocation": "Pune, India"
    }
    </script>
  </head>
  <body>
    <div id="scroll-bar"></div>
    <slot />

    <script>
      const bar = document.getElementById('scroll-bar');
      window.addEventListener('scroll', () => {
        const pct = window.scrollY / (document.documentElement.scrollHeight - window.innerHeight);
        if (bar) bar.style.width = (pct * 100) + '%';
      }, { passive: true });
    </script>

    <script>
      const observer = new IntersectionObserver(
        (entries) => entries.forEach(e => {
          if (e.isIntersecting) {
            e.target.classList.add('in-view');
            observer.unobserve(e.target);
          }
        }),
        { threshold: 0.15 }
      );
      document.querySelectorAll('.fade-up').forEach(el => observer.observe(el));
    </script>

    <button id="back-top" aria-label="Back to top" style="display:none;position:fixed;bottom:2rem;right:2rem;z-index:80;width:44px;height:44px;border-radius:50%;border:1px solid rgba(255,255,255,0.1);background:#0F1629;color:#22D3EE;cursor:pointer;align-items:center;justify-content:center;box-shadow:0 4px 16px rgba(0,0,0,0.4);">
      <svg width="18" height="18" viewBox="0 0 18 18" fill="none"><path d="M9 14V4M4 9l5-5 5 5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
    </button>
    <script>
      const btn = document.getElementById('back-top');
      window.addEventListener('scroll', () => {
        if (btn) btn.style.display = window.scrollY > 600 ? 'flex' : 'none';
      }, { passive: true });
      btn?.addEventListener('click', () => window.scrollTo({ top: 0, behavior: 'smooth' }));
    </script>
  </body>
</html>

<style is:global>
  @import '../styles/global.css';
</style>
